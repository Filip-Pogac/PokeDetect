# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data / validation
pydantic[email]==2.10.4
sqlalchemy==2.0.36

# Postgres driver, used when DATABASE_URL points at a hosted database (Neon).
# Unused on the SQLite default, but installed unconditionally so the same
# image works in both. [binary] ships prebuilt wheels - no libpq/compiler.
psycopg[binary]==3.2.3

# Auth
bcrypt==4.2.1
PyJWT==2.10.1

# Loads backend/.env into the environment. Already pulled in by
# uvicorn[standard], but declared here because config.py imports it directly.
python-dotenv==1.2.3

# HTTP client for the Pokemon TCG API
httpx==0.28.1

# Computer vision + OCR (deep-learning card recognition pipeline)
opencv-python-headless==4.10.0.84
numpy==2.1.3
easyocr==1.7.2

# Card matching: perceptual-hash visual verification + fuzzy text scoring
Pillow==11.0.0
imagehash==4.3.1
rapidfuzz==3.10.1
